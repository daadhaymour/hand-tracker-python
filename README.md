# hand-tracker-python

First time using the Mediapipe library experimenting with hand tracking using landmarks (circles placed at the joints) and lines connecting these landmarks.

# Packages
Python 3.11 

Mediapipe ver 0.10.21 - pip install mediapipe==0.10.21

OpenCV (cv2) - pip install opencv-python
